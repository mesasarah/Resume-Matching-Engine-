import React from 'react';
import '../css/CoursesRecommendations.css'; // Updated import path

const CoursesRecommendations = () => {
  return (
    <div className="courses-container">
      <h2>Courses Recommendations</h2>
      <p>Based on the job description, we will recommend courses here.</p>
      {/* Add logic to recommend courses */}
    </div>
  );
};

export default CoursesRecommendations;
